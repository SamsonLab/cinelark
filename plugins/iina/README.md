# CineLark IINA Bridge

This directory will contain the thin, provider-neutral IINA plugin.

Before implementation, resolve `BRIDGE-SEC-001` in
[`docs/interfaces/playback-bridge.md`](../../docs/interfaces/playback-bridge.md).
The current IINA WebSocket server must not be treated as loopback-only.
