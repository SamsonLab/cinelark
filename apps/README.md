# Applications

Planned targets:

- `macos/` — CineLark for Mac; the primary client and coordinator.
- `remote/` — future CineLark Remote for iPhone.

The Mac app must remain fully usable without the Remote.
