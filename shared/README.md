# Shared Product Sources

This directory contains platform-neutral source artifacts, not shared runtime
business code:

- `design/` — versioned color, typography, spacing, radius, and motion tokens.
- `brand/` — source vector logo, icon, and mascot assets.

Wire contracts belong in `specs/`. Generated Swift, Dart, or TypeScript output
must identify its source and must not be edited by hand.
